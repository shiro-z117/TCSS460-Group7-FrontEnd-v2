// ==============================|| DASHBOARD - CLEAN LAYOUT ||============================== //
// This layout bypasses the Material-UI DashboardLayout to allow pure Tailwind CSS

export default function DashboardCleanLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
